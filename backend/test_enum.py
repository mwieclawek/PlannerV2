from sqlalchemy import create_engine, MetaData, Table, Column, Integer, Enum
from sqlalchemy.dialects.postgresql import ENUM

# let's mock it or just look at docs
import sqlalchemy
print(sqlalchemy.__version__)
